// Create a Carousel

// Click function(s)
$(function() {
    // Slide to the left - Function
    $(".prev").click(slideLeft);

    // Slide to the right - Function
    $(".next").click(slideRight);
});